const state = () => ({
    
})
const mutations = {
   
}
const actions = {
    
}
const getters = {
    
}

export default function createDescriptionStore() {
    return {
        namespaced: true,
        state,
        getters,
        actions,
        mutations
    };
}