const state = () => ({
  
})
const mutations = {
  
}
const actions = {
    
}
const getters = {
   
}

export default function createHomeStore() {
    return {
        namespaced: true,
        state,
        getters,
        actions,
        mutations
    };
}