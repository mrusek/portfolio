const state = () => ({
   
})
const mutations = {
  
}
const actions = {
   
}
const getters = {
   
}

export default function createCvStore() {
    return {
        namespaced: true,
        state,
        getters,
        actions,
        mutations
    };
}